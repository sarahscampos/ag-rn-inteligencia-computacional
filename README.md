## Para Executar:
- Subir arquivo da base dados no formato CSV, nome do arquivo deverá ficar 'dados.csv' e deve estar no mesmo diretório do script.
- Instalar bibliotecas utilizadas, precisa ter [python instalado](https://www.python.org/downloads/) e depois executar o comando: `python -m pip install -r requirements.txt`
